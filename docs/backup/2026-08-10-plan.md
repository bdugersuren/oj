# 🚀 Мэдээлэлзүйн Олимпиадын Платформ — Шинэчлэгдсэн Сайжруулалтын Төлөвлөгөө (Active Plan)

Энэхүү баримт бичигт платформыг сайжруулах нарийвчилсан шийдлүүдийг хэрэгжүүлэх алхмууд болон тэдгээрийн явцыг тэмдэглэж байна.

---

## 📅 Гүйцэтгэлийн Шатууд ба Төлөв

- `[ ]` — Хийгдээгүй байгаа даалгавар
- `[/]` — Хийгдэж буй (In Progress)
- `[x]` — Бүрэн гүйцэтгэгдсэн

---

## 🛠️ ШАТ 1: DMOJ Холболтын Алдаануудыг засах (Бүтэц ба Аюулгүй байдал)

> **Зорилго**: DMOJ-ийн хөрвүүлэгчид (g++, python, java)-ийг ажиллуулж, `/problems` хавтас дахь тестүүдийг зөв унших боломжийг бүрдүүлнэ.

- [x] **1.1: Runtime Paths config нэгтгэх**
  - [x] `judge/bridge/entrypoint.sh` файлыг шинэчилж, `dmoj-autoconf`-оос гарсан `/judge-runtime-paths.yml` болон `/judge/judge.yml` (globs) тохиргоог нэгтгэн, `/tmp/merged_judge.yml` үүсгэн ажиллуулах.
  - [x] `judge/bridge/bridge.py` дотор config унших замыг `/tmp/merged_judge.yml` болгох.
- [x] **1.2: Docker Volume Permission засварлах**
  - [x] `judge/bridge/entrypoint.sh` дотор ажиллаж буй `judge` хэрэглэгчид `/problems` хавтсанд унших, бичих эрхийг баталгаажуулах (`chmod` / `chown` тохиргоог зөв оруулах).
- [x] **1.3: DMOJ Bridge-ийн холболтыг шалгах Health-check API**
  - [x] `backend/app/api/v1/endpoints/admin_settings.py` (эсвэл шинээр) дотор DMOJ Bridge-үүд рүү холболт үүсгэж, бэлэн эсэхийг нь шалгадаг статус API бичих.

---

## 💻 ШАТ 2: Хөгжүүлэлтийн Subprocess Sandbox (Dockerless Local Judge)

> **Зорилго**: DMOJ контейнергүйгээр, локал орчинд кодын зөв/бурууг шалгах хурдан, хөнгөн sandbox хөдөлгүүрийг FastAPI backend дотор хэрэгжүүлэх.

- [x] **2.1: Local Judge үйлчилгээ үүсгэх**
  - [x] `backend/app/services/local_judge.py` файлыг үүсгэх.
  - [x] Temp хавтас үүсгэн, сурагчийн кодыг хадгалж, C++ (`g++`), Python (`python3`), Java (`javac`/`java`) хэлээр локал систем дээр ажиллуулах логик бичих.
- [x] **2.2: Хязгаарлалтуудыг хянах (psutil)**
  - [x] `psutil` ашиглан хугацаа (TLE), санах ойн хязгаарыг (MLE) хянан процессыг зогсоох логик нэмэх.
- [x] **2.3: API-д холбох**
  - [x] `submissions.py` endpoint-д `is_sample_test=True` эсвэл `ENABLE_JUDGE=False` үед Celery-ийг алгасан шууд `LocalSubprocessJudge`-ээр бодож хариуг буцаадаг болгох.

---

## 🎨 ШАТ 3: Багшийн Вэб Workspace (Polygon)

> **Зорилго**: Багш хөтөч дээрээсээ бодлого, тест үүсгэгч (generator), зөв шийдэл (model solution), custom checker зэргийг удирдаж, автоматаар MinIO нийтэлдэг болох.

- [x] **3.1: Backend-д Бодлого багцлах API**
  - [x] Багшийн вэбээс ирүүлсэн файлуудыг (`statement.md`, `solution.cpp`, `generator.cpp`, `checker.py`, `init.yml`) уншиж санах ойд ZIP үүсгэн, MinIO-ийн `oj-private-problems` саванд `{code}/cases.zip` нэрээр хадгалах API бичих.
- [x] **3.2: Тест Кэйс автомат үүсгэгч (Generator & Solution Runner)**
  - [x] Багш вэб дээр өгсөн параметрийн дагуу `generator.cpp`-ийг ажиллуулан `.in` үүсгэж, `solution.cpp`-ийг ажиллуулан `.out` файлыг автоматаар үүсгэдэг Backend логик бичих.
- [x] **3.3: Frontend UI хөгжүүлэлт**
  - [x] `/teacher/problems/[code]/workspace` шинэ хуудас үүсгэх.
  - [x] Monaco Editor бүхий табууд нэмэх: "Өгүүлбэр", "Зөв шийдэл", "Тест Генератор", "Тестүүд (Grid)", "Custom Checker".

---

## 🏆 ШАТ 4: Testlib болон Custom Checker дэмжлэг

> **Зорилго**: C++ хэлний Testlib ашиглан бичсэн нарийн checker, validator-уудыг DMOJ болон локал Judge дээр бүрэн ажиллуулах.

- [x] **4.1: Testlib-ийг системд байршуулах**
  - [x] `testlib.h` толгой файлыг татан авч, DMOJ Judge-ийн Docker-т `/usr/include/` эсвэл хөрвүүлэгчийн замд байршуулах.
- [x] **4.2: C++ Custom Checker-ийг хөрвүүлж ажиллуулах**
  - [x] `checker.cpp` байвал DMOJ болон Local Sandbox дотор эхэлж хөрвүүлэн, тест тус бүрт уг `checker.out`-оор сурагчийн хариуг шалгуулдаг логик нэмэх.

---

*Сүүлд шинэчлэгдсэн огноо: 2026-08-10 | Хариуцагч: Antigravity AI*
