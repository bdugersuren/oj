# One-time, loopback-only bootstrap override for NPM and admin tools.
# Never use this file as the steady-state public proxy deployment.
services:
  npm:
    ports: !override
      - "127.0.0.1:8080:80"
      - "127.0.0.1:81:81"
      - "127.0.0.1:8443:443"

